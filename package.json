{
  "name": "desarrollo-aplicaciones-front",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "start:dev": "NODE_ENV=development expo start -c",
    "start:dev:tunnel": "NODE_ENV=development expo start -c --tunnel",
    "start:test": "NODE_ENV=test expo start -c",
    "start:test:tunnel": "NODE_ENV=test expo start -c --tunnel",
    "start:prod": "NODE_ENV=production expo start -c",
    "start:prod:tunnel": "NODE_ENV=production expo start -c --tunnel",
    "android:dev": "NODE_ENV=development expo start -c --android",
    "ios:dev": "NODE_ENV=development expo start -c --ios",
    "web:dev": "NODE_ENV=development expo start -c --web"
  },
  "dependencies": {
    "expo": "~54.0.33",
    "expo-status-bar": "~3.0.9",
    "react": "19.1.0",
    "react-native": "0.81.5"
  },
  "devDependencies": {
    "@types/react": "~19.1.0",
    "typescript": "~5.9.2"
  },
  "private": true
}
